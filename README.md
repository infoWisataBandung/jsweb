```
git add .
git commit -m "commits"
git tag v1.0.0
git push origin --tags
```
https://cdn.jsdelivr.net/gh/infoWisataBandung/jsweb/

```
git tag
git commit -m "commits"
git tag v1.0.4
git push --tags
```